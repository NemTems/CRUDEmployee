failedToCreateEmployee:
  type: object
  properties:
    code:
      type: integer
      example: 400
    message:
      type: string
      example: "Exception occurred on attempt to create an employee"
failedToSaveEmployee:
  type: object
  properties:
    code:
      type: integer
      example: 400
    message:
      type: string
      example: "Exception occurred on attempt to save an employee"
failedToFindEmployee:
  type: object
  properties:
    code:
      type: integer
      example: 400
    message:
      type: string
      example: "Employee not found"
failedToDeleteEmployee:
  type: object
  properties:
    code:
      type: integer
      example: 400
    message:
      type: string
      example: "Exception occurred on attempt to delete employee"

failedToSaveSubordinate:
  type: object
  properties:
    code:
      type: integer
      example: 400
    message:
      type: string
      example: "Exception occurred on attempt to save a subordinate"
failedToFindSubordinate:
  type: object
  properties:
    code:
      type: integer
      example: 400
    message:
      type: string
      example: "Subordinate not found"

wrongArgumentsPassed:
  type: object
  properties:
    code:
      type: integer
      example: 400
    message:
      type: string
      example: "Wrong arguments were passed"
